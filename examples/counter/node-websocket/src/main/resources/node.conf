include "node-defaults"
include "websocket-server-defaults"

node {

  id = "Node1"

  services {
    ws1 = "rs.service.websocket.WebsocketService"
  }

  cluster {

    core-nodes = [
      "localhost:2801"
    ]

  }
}

